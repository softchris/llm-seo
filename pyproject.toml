[project]
name = "llm-seo"
version = "0.1.0"
description = "LLM SEO Analyzer — evaluate whether a website is agent-ready"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
]
